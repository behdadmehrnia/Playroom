# Heavy deps for offline PDF indexing only (not needed by the FastAPI runtime image).
# Install into the same venv as requirements.txt:
#   pip install -r requirements.txt -r requirements-indexer.txt
#
# MinerU downloads OCR/layout models on first run.
# If HuggingFace is unreachable: export MINERU_MODEL_SOURCE=modelscope

-r requirements.txt
mineru[pipeline]>=2.0.0
