services:
  textbook-service:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      TEXTBOOK_DATA_DIR: /app/data
      # TEXTBOOK_API_KEY: "your-secret-key"
    restart: unless-stopped
