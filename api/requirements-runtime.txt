# Runtime dependencies for the Yar Kids API (chat + embedded textbook retrieve).
# Heavy MinerU indexer deps are intentionally omitted — mount a prebuilt
# index/pages volume, or install api/requirements.txt in a separate indexer image.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
httpx>=0.27.0
sse-starlette>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pillow>=10.0.0
