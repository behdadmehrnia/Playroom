# Root requirements - includes all dependencies for both API and textbook-service
# API requirements
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
httpx>=0.27.0
sse-starlette>=2.1.0
python-dotenv>=1.0.0

# Textbook service requirements
pymupdf>=1.24.0
pillow>=10.0.0
pytesseract>=0.3.10
python-multipart>=0.0.9

# Indexer requirements (CPU-only)
numpy<2
mineru[pipeline]>=2.0.0
opencv-python-headless>=4.9.0